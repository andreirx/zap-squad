[package]
name = "zapsquad-adapters"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
zapsquad-core = { path = "../core" }
zap-engine = { path = "../../zap-engine/crates/zap-engine" }
rhai = { workspace = true }
glam = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
