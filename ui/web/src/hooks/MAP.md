# Hooks (`ui/web/src/hooks/`)

## Role: Shared Logic
Custom React hooks that encapsulate reusable logic.

## Key Hooks
- **useHotReload**: Manages the hot-reloading of game assets (scripts, levels) from the editors into the running game engine.

## Dependencies
- Does not depend on UI components.
- May depend on `storage` or `services`.
