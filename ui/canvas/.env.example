# Base URL for baked assets (atlas PNGs, assets.json, manifest.json).
# Local dev: served from ui/web/public/assets/ via Vite middleware (see vite.config.ts).
# Production: S3 bucket via CloudFront.
VITE_ASSETS_URL=/assets
